import React from 'react';
import { NavigationContainer } from '@react-navigation/native';

export default function AppNavigator() {
  return (
    <NavigationContainer>
      {/* navigator setup (e.g. Stack or Tab navigators with lazy screen loading)... */}
    </NavigationContainer>
  );
}

